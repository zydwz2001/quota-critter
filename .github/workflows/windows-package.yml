name: Windows package

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Build NSIS installer
        # RUSTFLAGS=-C save-temps 防御某些 MSVC + rustc 版本下编译 proc-macro 时
        # 链接完成后不退出（WaitForSingleObject 竞态）。代价是 target 残留 .o/.bc 中间文件。
        env:
          RUSTFLAGS: "-C save-temps"
        run: npm run tauri build -- --bundles nsis --ci

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: quota-critter-windows-x64
          path: src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
